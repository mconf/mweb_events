= simple_form_for(@event) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.label :start_on
    = f.text_field :start_on
    = f.input :address
    = f.hidden_field :description

  #epiceditor

  .form-actions
    = f.button :submit
