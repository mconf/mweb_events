- if can?(:register, @event) && !@event.past?
  %h2
    = t('.title')

  %section.event-register
    = link_to t('.button'), new_event_participant_path(@event), :class => 'btn btn-success large main'
